{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "engines": {
        "node": ">=14.x"
    },
    "scripts": {
        "dev": "yarn && lerna link && lerna bootstrap && lerna exec --no-bail -- yarn dev"
    }
}
